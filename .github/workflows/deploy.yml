name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Hostinger Deploy Webhook
        run: |
          curl -X POST https://webhooks.hostinger.com/deploy/78c45bd4eb93c661dd6fdc05df30774b
